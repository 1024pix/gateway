{
  "name": "pix-gateway",
  "version": "1.0.0-beta",
  "description": "Pix API Gateway",
  "main": "index.js",
  "scripts": {
    "start": "node bin/www",
    "start:watch": "nodemon bin/www",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "pix",
  "license": "ISC",
  "dependencies": {
    "@hapi/hapi": "^20.0.3"
  },
  "peerDependencies": {
    "nodemon": "2.0.6"
  }
}
